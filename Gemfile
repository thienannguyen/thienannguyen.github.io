# Gemfile for GitHub Pages. Run: bundle install && bundle exec jekyll serve
source "https://rubygems.org"

gem "jekyll", "~> 3.9"
# GitHub Pages uses this; uncomment to match their env exactly:
# gem "github-pages", group: :jekyll_plugins
